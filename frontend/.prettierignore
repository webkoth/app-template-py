# Сгенерированное и привезённое: форматирует их не человек.
#
# schema.d.ts печатает openapi-typescript при каждом `make openapi`, и любая
# правка формата исчезнет на следующей генерации — зато `make check-openapi`
# начнёт видеть расхождение там, где его нет.
src/api/schema.d.ts
# components/ui кладёт shadcn. Переформатировав их, мы получим шумный дифф
# при каждом обновлении компонента и потеряем возможность сверить с тем,
# что отдаёт CLI.
src/components/ui/
dist/
