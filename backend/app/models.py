"""Реестр таблиц: единственное место, где перечислены все модели.

Метаданные `Base` наполняются импортом — таблица попадает в них, только если
её модуль кто-то импортировал. Потребителей у метаданных два: `alembic/env.py`
собирает по ним миграции, `tests/conftest.py` — схему тестовой базы. Раньше
каждый вёл свой список, и списки уже разошлись: `User` был в одном и
приезжал в другой попутно, вместе с `UserStatus`.

Цена расхождения — молчаливая. Модель очереди добавили в обвязку тестов и
забыли в `env.py`: `make revision` выдал бы ПУСТУЮ миграцию, файл ревизии
создался бы, прогон остался бы зелёным, а таблицы в базе не появилось бы.
Причину искали бы в Alembic. Хуже того, положи кто-нибудь миграцию руками —
следующая автогенерация предложила бы таблицу удалить, потому что в
метаданных её нет.

Поэтому список один и здесь. Добавляя фичу с таблицей, дописывай строку
сюда; `tests/unit/test_models_registry.py` не даст об этом забыть — он
обходит пакет и сверяет найденное с перечисленным.
"""

from app.core.audit import AuditLog
from app.core.jobs import Job
from app.core.users import User
from app.features.datasets.models import Dataset, DatasetRow, ModelArtifact
from app.features.expenses.models import Expense

# Имена перечислены явно, а не просто импортированы ради побочного эффекта:
# так линтер не сочтёт импорты лишними, а читатель видит состав схемы одним
# взглядом, не разбирая цепочку импортов.
__all__ = [
    "AuditLog",
    "Dataset",
    "DatasetRow",
    "Expense",
    "Job",
    "ModelArtifact",
    "User",
]
